schemaVersion: "1"
namespace: workers
owner:
  team: platform
  contact: platform@example.com
services:
  - label: com.example.shared
    domain: gui
    user: kristian
    program: /usr/bin/env
    programArguments:
      - /usr/bin/env
      - "true"
