schemaVersion: "1"
namespace: app
owner:
  team: platform
  contact: platform@example.com
services:
  - label: com.example.health
    domain: gui
    user: kristian
    program: /usr/bin/env
    programArguments:
      - /usr/bin/env
      - "true"
    health:
      type: http
      intervalSeconds: 10
      timeoutSeconds: 2
      url: not-a-url
