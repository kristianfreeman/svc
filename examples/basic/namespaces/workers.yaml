schemaVersion: "1"
namespace: workers
owner:
  team: platform
  contact: platform@example.com
services:
  - label: com.example.worker.queue
    domain: gui
    user: you
    program: /usr/local/bin/node
    programArguments:
      - /usr/local/bin/node
      - /srv/worker/queue.js
    environment:
      APP_ENV: production
      WORKER_QUEUE: email
